<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        Base theme uses Theme.Material3.DayNight.NoActionBar.
        On Android 12+ DynamicColors (applied in PensieveApp) overlays this
        with the user's wallpaper-derived colour palette automatically.
        On Android 11 and below the seed colour (#6750A4) in colors.xml is used.
    -->
    <style name="Base.AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Edge-to-edge -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="q">false</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="q">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges</item>
    </style>
    <style name="AppTheme" parent="Base.AppTheme" />
</resources>
